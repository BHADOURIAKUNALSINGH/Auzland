{
  "name": "contact-form-lambda",
  "version": "1.0.0",
  "description": "Lambda function to handle contact form submissions via AWS SES",
  "main": "index.js",
  "dependencies": {
    "aws-sdk": "^2.1450.0"
  },
  "scripts": {
    "deploy": "zip -r contact-form-lambda.zip ."
  }
}
